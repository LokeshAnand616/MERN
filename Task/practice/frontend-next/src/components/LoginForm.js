'use client'
import { useMutation } from "@apollo/client";
import { LOGIN_USER } from "@/graphql/Mutation/signupMutation.js";

const LoginForm=()=>{
    return(
        <>
        </>
    )
}

export default LoginForm;